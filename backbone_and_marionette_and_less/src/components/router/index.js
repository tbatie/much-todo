/**
 * what does this do?  It lets you import like so somewhere else: import Router from '.../components/router'
 * That's opposed to fully qualifying the file like: import Router from '...../components/router/router'
 * It also lets you combine your exports for a folder in one area (if you have multiple files in this folder that export things)
 */
export { default } from "./router";
